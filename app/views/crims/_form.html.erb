<%= form_for(crim) do |f| %>
  <% if crim.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(crim.errors.count, "error") %> prohibited this crim from being saved:</h2>

      <ul>
      <% crim.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :zip %>
    <%= f.text_field :zip %>
  </div>

  <div class="field">
    <%= f.label :date %>
    <%= f.text_field :date %>
  </div>

  <div class="field">
    <%= f.label :plate %>
    <%= f.text_field :plate %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
